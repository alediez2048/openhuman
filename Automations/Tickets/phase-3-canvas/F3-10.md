# F3-10 Primer: Phase 3 Hero E2E + DEVLOG Closure + ADR Drift Audit

**For:** New coding-agent session
**Project:** OpenHuman — Workflows & Automations
**Previous work:** F3-1..F3-9 ship every Phase 3 deliverable.

---

## What Is This Ticket?

Closes Phase 3. Lands the hero E2E demonstrating canvas-driven creation of a fan-out workflow + the "Phase 3 Complete" DEVLOG section + the ADR drift audit. Same role F-15 / F2-16 played for prior phases.

### Why It Matters

- Without the E2E, "Phase 3 ships" is a promise. With it, the canvas + fan-out + approval surface have a tested guarantee.

---

## What This Ticket Must Accomplish

### Goal

- **Hero E2E** at `app/test/e2e/specs/workflows-phase-3-canvas.spec.ts`:
    1. Fresh workspace.
    2. Open `/workflows/new`, switch to Canvas tab.
    3. Drag in: Manual trigger → AgentPrompt (drafts an email per attendee) → FanOut (5 attendees) → child ChannelMessage (sends Slack DM per attendee) → AwaitHumanApproval (one approval gates the next send).
    4. Wire the edges via drag.
    5. Configure each node via the drawer.
    6. Click Save & Enable.
    7. Manually fire via `workflows_run_now`.
    8. Watch the live highlighting (asserted via DOM state updates).
    9. Approve the gate; the run completes.
    10. Asserts: 5 child ChannelMessage step rows + 1 AwaitHumanApproval row + final agg row.

- **Phase 3 DEVLOG closure** at `Automations/Tickets/phase-3-canvas/DEVLOG.md`:
    - Summary section per ticket with commit SHAs.
    - Phase 3 DoD checklist.
    - ADR drift audit for the 5 Phase 3-relevant ADRs + any new ADRs filed during the phase (canvas library lock, fan-out concurrency policy, etc.).

- **Capability catalog update** in `src/openhuman/about_app/catalog.rs`: `view_workflow_canvas`, `build_with_canvas`, `approve_human_gated_run`, `parallel_fan_out`, `transform_node`.

- **Bump `CURRENT_PHASE = 3`** in `ops.rs` + propose tools.

### Deliverables Checklist

#### A. Implementation

- [ ] Hero E2E spec.
- [ ] DEVLOG closure.
- [ ] README backfill with SHAs.
- [ ] `about_app` Phase 3 capabilities.
- [ ] `CURRENT_PHASE = 3` constants.

#### B. Tests

- [ ] Hero E2E passes on macOS local + Linux CI.
- [ ] Full pre-merge gauntlet green.

---

## Phase 3 DoD checklist

- [ ] All 10 tickets merged.
- [ ] Hero E2E passes.
- [ ] All Phase 1+2+3 templates validate.
- [ ] Live highlighting verified (manual sanity if E2E can't catch state pulses).
- [ ] Approval banner + OS notification verified manually.
- [ ] DEVLOG has the closure section + ADR drift audit.

---

## Estimated Time

~8 hours.

---

## After Phase 3

- **Phase 4** (deeper external-platform integrations) — gated on
  adoption metrics. Each its own project per `prd.md §5.4`:
    - OpenHuman as a published n8n custom node.
    - OpenHuman as a Zapier-directory app.
    - OpenHuman as an IFTTT applet.
    - OpenHuman as a Make custom app.
- Possible Phase 5: shared workflows / templates marketplace.
  Currently a non-goal per `prd.md §7`.
